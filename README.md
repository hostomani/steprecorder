# steprecorder

This is a step recorder for mac since there is no available free step recorders 

Create virtual environment first 

```
virtualenv venv
```

Then install dependencies
```
pip install -r requirements.txt
```

Then run it 
```
python app.py
```
